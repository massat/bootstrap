---

- homebrew:
    name:
      - pyenv

- lineinfile: dest=~/.myzshrc line={{ item }}
  with_items:
    - export PYENV_ROOT="$HOME/.pyenev"
    - export PATH="$PYENV_ROOT/bin:$PATH"
    - eval "$(pyenv init -)"
